services:

  ollama:
    image: ollama/ollama:latest
    container_name: code-agent
    restart: unless-stopped

    ports:
      - "11434:11434"

    volumes:
      - ollama_data:/root/.ollama

volumes:
  ollama_data: