CREATE TABLE IF NOT EXISTS SPRING_AI_CHAT_MEMROY(
	"CONVERSATION_ID" VARCHAR(36) NOT NULL,
	"TIMESTAMP" TIMESTAMP NOT NULL,
	"CONTENT" TEXT,
	"TYPE" VARCHAR(20) NOT NULL,
	PRIMARY KEY (CONVERSATION_ID, TIMESTAMP)
);